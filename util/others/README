# ================================================================================================

# 	Version:	1.0
# 	File:		README
# 	Author:		Yunshuang XIAO

# ------------------------------------------------------------------------------------------------

#   1. use "find_elite_configs" to find out each elite configuration from the standard output file
# of each target experiment. Get the index of elites and search for the correspoding configuration
# based on the "config.log" of the target experiment.
#   To run this file, the address of the target experiment should be provided.
#   eg: ./find_elite_configs /path/to/crace-2.11/acotsp

#   2. use "elites_on_instances" to test each elite configuration on each instance.
#   To run this file, the address of the target experiment and the instances should be provided.
#   eg: ./elites_on_instances /path/to/crace-2.11/acotsp /path/to/instances/file

# ================================================================================================

# For crace results:
#   1. use ./find_elite_configs_crace /path/to/crace 
#      to find out all elite configurations appear in the crace procedure. 
#   2. use ./elites_on_instances /path/to/crace /path/to/instance
#      to execute elite(s) on the provided instances. 

# For irace results:
#   1. use R script "R2CSV.r" to find out the elitist and elite configurations
#      and storage the information in the format used in crace results ("elites.log").
#   2. use ./find_elite_configs_irace /path/to/irace
#      to find out all elite configurations appear in the irace procedure. 
#   3. new R2CSV.r can find all elites, no need to use ./find_elite_configs_irace

